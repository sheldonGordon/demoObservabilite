spring:
  application:
    name: demo-observabilite-batch
  main:
    web-application-type: none
  batch:
    jdbc:
      initialize-schema: always
    job:
      enabled: true
      name: demoJob
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/demo_observabilite}
    driver-class-name: org.postgresql.Driver
    username: ${SPRING_DATASOURCE_USERNAME:demo}
    password: ${SPRING_DATASOURCE_PASSWORD:demo}

logging:
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] [%X{trace_id:-},%X{span_id:-},%X{frontend_session_id:-}] %logger{36} - %msg%n"

demo:
  backend:
    base-url: ${BACKEND_BASE_URL:http://localhost:8080}
    auth:
      username: ${BACKEND_AUTH_USERNAME:demo}
      password: ${BACKEND_AUTH_PASSWORD:demo}

